export enum ContractStatus {
  APPROACHING = 'APPROACHING',
  ACTIVE = 'ACTIVE',
  EXPIRED = 'EXPIRED'
}

export enum ContractRecordStatus {
  ENABLE = 'ENABLE',
  DISABLE = 'DISABLE',
  DELETE = 'DELETE'
}
